name: Build/release

on: push

jobs:
  release:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 22

      - name: Download VM image (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          mkdir -p vendor/v86/images
          curl -fsSL -o vendor/v86/images/alpine.img "https://s3.us-west-2.wasabisys.com/deepclause-public/alpine.img?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=LES0W5CHN5VV2VM18AR7%2F20251112%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20251112T144116Z&X-Amz-Expires=43200&X-Amz-Signature=723a9b9dbbd321851709b32574a416d06e72944ea64d8b873a347859976c812d&X-Amz-SignedHeaders=host&x-id=GetObject"
        shell: bash

      - name: Download VM image (Windows)
        if: runner.os == 'Windows'
        run: |
          New-Item -ItemType Directory -Force -Path vendor\v86\images | Out-Null
          Invoke-WebRequest -Uri "https://s3.us-west-2.wasabisys.com/deepclause-public/alpine.img?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=LES0W5CHN5VV2VM18AR7%2F20251112%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20251112T144116Z&X-Amz-Expires=43200&X-Amz-Signature=723a9b9dbbd321851709b32574a416d06e72944ea64d8b873a347859976c812d&X-Amz-SignedHeaders=host&x-id=GetObject" -OutFile vendor\v86\images\alpine.img
        shell: pwsh
        
      - name: Build/release Electron app
        uses: samuelmeuli/action-electron-builder@v1
        with:
          # GitHub token, automatically provided to the action
          # (No need to define this secret in the repo settings)
          github_token: ${{ secrets.github_token }}

          # If the commit is tagged with a version (e.g. "v1.0.0"),
          # release the app after building
          release: true
